[project]
name = "health-watchdog"
version = "0.1.0"
description = "Health Watchdog Service"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
